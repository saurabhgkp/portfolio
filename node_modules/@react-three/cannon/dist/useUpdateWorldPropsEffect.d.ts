import type { ProviderProps } from './Provider';
declare type useUpdateWorldPropsEffect = Pick<ProviderProps, 'gravity' | 'tolerance' | 'step' | 'iterations' | 'broadphase' | 'axisIndex'> & {
    worker: Worker;
};
export declare function useUpdateWorldPropsEffect({ worker, gravity, tolerance, step, iterations, broadphase, axisIndex, }: useUpdateWorldPropsEffect): void;
export {};
